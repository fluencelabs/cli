#!/usr/bin/env node

const oclif = require("@oclif/core");
const { createErrorPromise } = require("../src/countlyInterceptor");

oclif
  .run()
  .then(require("@oclif/core/flush"))
  .catch((error) => createErrorPromise(error, require("@oclif/core/handle")));
